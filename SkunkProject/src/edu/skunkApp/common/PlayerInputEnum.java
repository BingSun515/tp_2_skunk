package edu.skunkApp.common;

// Roll Choices :: Y (roll again) / N (stop my Turn) / HELP (Display game rules) / R (Round Score) / M (My Score)
public enum PlayerInputEnum {
	CANNOT_PLAY,
	HELP,
	M,
	N,
	R,
	Y
}
